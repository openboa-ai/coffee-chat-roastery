# Canonical Roastery contract

This directory is reserved for the future canonical Roastery contract. The
migration shell deliberately ships no schema, seed, validator, publication
contract, or compatibility layer.

The official repository remains Bean-free. A future initialized owner fork may
contain exactly one `roastery/**` root; its Bean content is fixed CC BY 4.0 and
does not grant repository files authority over the official package.
